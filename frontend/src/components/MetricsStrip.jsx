/**
 * MetricsStrip — compact top-of-page KPI bar.
 * Per CLAUDE.md Section 22:
 * - KPIs: RPS, P95 latency ms, cache hit %, cost/hour USD, avg quality score
 * - Sourced from Prometheus via prometheus.js query wrapper
 * - Refreshes every 10 seconds
 * Props: { tenantId: string | null }
 */
export default function MetricsStrip({ tenantId }) {
  throw new Error("Implement per CLAUDE.md Section 22 (Week 7)");
}

/**
 * PolicyCard — compact policy summary for a single tenant/domain.
 * Per CLAUDE.md Section 22:
 * - Displays: policy_id, schema_version, budget profiles (low/medium/high/critical)
 * - Displays: limits (RPM, TPH, cost cap), SLO thresholds, cache config
 * Props: { policy: object }
 */
export default function PolicyCard({ policy }) {
  throw new Error("Implement per CLAUDE.md Section 22 (Week 7)");
}
